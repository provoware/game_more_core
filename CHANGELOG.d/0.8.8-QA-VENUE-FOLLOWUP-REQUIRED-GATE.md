# 0.8.8 QA – Venue Follow-up Required Gate

- verschiebt die Venue-Folgeideen-Regression aus dem nicht-required `tests/quality`-Pfad in die vom Required Gate `repository-health` entdeckte Suite `tests/repository`;
- korrigiert die Regression auf die tatsächlich dokumentierte Formulierung `zweite Bonusengine`;
- ergänzt eine einfache spätere Verbesserungsidee für einen read-only Receipt-Hinweis, der nur bestätigte Venue-Ursachen anzeigen darf;
- keine Runtime-, Settlement-, Property-, Balance-, Save-, Journal-, Browser- oder Gameplay-Produktlogik geändert.
