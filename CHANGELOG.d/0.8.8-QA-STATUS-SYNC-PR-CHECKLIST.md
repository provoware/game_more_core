# 0.8.8-QA-STATUS-SYNC-PR-CHECKLIST

- `tools/status_sync.py suggest-markdown` erzeugt jetzt eine rein lesende, direkt kopierbare Reparaturcheckliste für den normalen Status-Sync-PR.
- Die Checkliste nennt exakt beide Markdown-Anker und die zwei `PROJEKTSTATUS.json`-Ankerfelder, schützt `remote_validation` ausdrücklich vor Umdeutung und erinnert an `STATUS SYNC PASS` plus `/safe-merge`.
- Eine fokussierte Quality-Regression sichert den Output; Gameplay-, Balance-, Save-, Journal-, Venue- und Browserlogik bleiben unverändert.